<%= form_with(model: shopping) do |form| %>
  <% if shopping.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(shopping.errors.count, "error") %> prohibited this shopping from being saved:</h2>

      <ul>
        <% shopping.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :buy %>
    <%= form.text_field :buy %>
  </div>

  <%#div class="field">
    <%= form.label :user_id %>
    <%#%= form.number_field :user_id %>
  <%#/div%>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
