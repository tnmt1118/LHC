<p id="notice"><%= notice %></p>
<div class="blocktitle">
<h1>ブロック</h1>
</div>

<table>
  <thead>
    <tr>
      <th class="blockitiran">ブロック一覧</th>
      <!--th>User</th-->
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @blocks.each do |block| %>
      <tr>
        <td class="blockitiran1"><%= block.block %></td>
        <td><%= block.user_id %></td>
        <td class="blockitiran1"><%= link_to 'Show', block %></td>
        <td class="blockitiran1"><%= link_to 'Edit', edit_block_path(block) %></td>
        <td class="blockitiran1"><%= link_to 'Destroy', block, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'ブロック追加', new_block_path %>

